[project]
name = "my-first-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pytest>=9.0.3",
    "requests>=2.34.2",
    "sqlmodel>=0.0.38",
    "streamlit>=1.57.0",
]

[tool.uv.workspace]
members = [
    "note-api",
]
